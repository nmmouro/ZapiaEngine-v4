import { SCHEMA_ABASTECIMENTO } from "../schemas/abastecimentos.js";
import { carregarRegistro, carregarRelacionados, iniciarViewBase, renderizarCampos, renderizarAnexo, renderizarLista, escapar } from "./viewService.js";
export async function iniciar(){const id=new URLSearchParams(location.search).get("id");const content=iniciarViewBase({titulo:"Abastecimento",subtitulo:id||""});try{const r=await carregarRegistro("abastecimento",id);if(!r)throw new Error("Abastecimento não encontrado.");let lanc=[];if(r.id_lancamento)lanc=await carregarRelacionados("lancamentos","id",r.id_lancamento);content.innerHTML=`<section class="view-section"><h2>Dados do abastecimento</h2>${renderizarCampos(r,SCHEMA_ABASTECIMENTO,{excluir:["id","id_lancamento","imagem","criado_em","atualizado_em"]})}</section><section class="view-section"><h2>Documento / Foto</h2><div class="view-attachments">${r.imagem?renderizarAnexo(r.imagem,"Imagem da nota / abastecimento"): '<div class="view-empty">Nenhum documento ou foto.</div>'}</div></section>${renderizarLista("Ocorrência vinculada",lanc,null,["id","data","hora","veiculo","empregado_matricula","passageiro_setor_motivo","status"])}`;}catch(e){content.innerHTML=`<section class="view-section"><div class="view-empty">${escapar(e.message)}</div></section>`;}}
