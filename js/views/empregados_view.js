import { SCHEMA_EMPREGADOS } from "../schemas/empregados.js";
import { carregarRegistro, carregarRelacionados, iniciarViewBase, renderizarCampos, renderizarAnexo, renderizarLista, escapar } from "./viewService.js";
export async function iniciar(){const id=new URLSearchParams(location.search).get("id");const content=iniciarViewBase({titulo:"Empregado",subtitulo:id||""});try{const r=await carregarRegistro("empregados",id);if(!r)throw new Error("Empregado não encontrado.");const lanc=await carregarRelacionados("lancamentos","id_empregado",r.id);content.innerHTML=`<div class="view-hero"><div class="view-hero-media">${r.foto?renderizarAnexo(r.foto,"Foto do empregado"): '<div class="view-placeholder">Sem foto cadastrada</div>'}</div><section class="view-section"><h2>${escapar(r.empregado||r.id)}</h2>${r.status?`<div class="view-alerts"><span class="view-status neutral">Status: ${escapar(r.status)}</span></div>`:''}${renderizarCampos(r,SCHEMA_EMPREGADOS,{excluir:["id","foto","criado_em","atualizado_em"]})}</section></div><section class="view-section"><h2>Foto</h2><div class="view-attachments">${r.foto?renderizarAnexo(r.foto,"Foto"): '<div class="view-empty">Sem foto.</div>'}</div></section>${renderizarLista("Lançamentos do empregado",lanc,null,["id","data","hora","veiculo","passageiro_setor_motivo","status"])}`;}catch(e){content.innerHTML=`<section class="view-section"><div class="view-empty">${escapar(e.message)}</div></section>`;}}
